<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jrbac.dao.TeacherDao">

	<select id="queryAll" resultType="Teacher">
		SELECT
		id,`name`,`subject`,img,info,`order` FROM jrbac_teacher ORDER BY
		`order` ASC
	</select>

	<select id="queryById" resultType="Teacher">
		SELECT
		id,`name`,`subject`,img,info,`order` FROM jrbac_teacher
		WHERE id =
		#{id}
	</select>

	<insert id="add">
		INSERT INTO
		jrbac_teacher(id,`name`,`subject`,img,info,`order`)
		VALUES
		(#{teacher.id},#{teacher.name},#{teacher.subject},#{teacher.img},#{teacher.info},#{teacher.order})
	</insert>

	<update id="update">
		UPDATE
		jrbac_teacher
		SET
		`name` = #{teacher.name},
		`subject` =
		#{teacher.subject},
		<if test="teacher.img != null">
			img = #{teacher.img},
		</if>
		info = #{teacher.info},
		`order` = #{teacher.order}
		WHERE id =
		#{teacher.id}
	</update>
	<delete id="delete">
		DELETE FROM jrbac_teacher WHERE id = #{id}
	</delete>
</mapper>